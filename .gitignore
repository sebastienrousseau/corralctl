# macOS
.DS_Store

# Linux
*~
.nfs*

# Editors
*.swp
*.swo
.idea/
.vscode/

# Build artifacts
/corral
/corralctl
coverage.*
dist/

# Generated docs site (regenerated by .github/workflows/docs.yml on every push)
/public/index.html

# Runtime sidecar written next to each cloned repo's .git/. The file is meant
# to live inside individual clones; if you run `corralctl` from this source
# tree during development it lands at the project root by accident — keep it
# out of version control.
.corral-state.json

# Scratch trees from scripts/example_check.go, if cleanup is ever prevented.
.example-check-*/
public/
public.build-tmp/
docs-site/content/reference.md

# Generated manpages and completions (see scripts/gen_docs.go)
build/

# Compiled test binaries. `go test -c` and `go test -o` leave these behind, and
# one (mcp.test, 16.6MB) was committed before this rule existed.
*.test
